# 路径约定示例。完整构建/闸门请从 jiangsu-zikao-aio 复制脚本。
# 环境变量：${VAR_NAME:default}

[paths]
courses_dir     = "${ZIKAO_COURSES_DIR:content/example/courses}"
majors_dir      = "${ZIKAO_MAJORS_DIR:content/example/majors}"
site_dir        = "${ZIKAO_SITE_DIR:site}"

[source_link_monitor]
baseline        = "${ZIKAO_BASELINE:ops/example/source-links.baseline.json}"
report          = "${ZIKAO_REPORT:site/source-link-report.md}"
summary         = "${ZIKAO_SUMMARY:site/source-link-report.json}"
